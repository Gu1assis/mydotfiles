# Estrutura recomendada

Deixe os scripts na pasta config e adicione por meio de Symlink ao seu 
shell numa pasta que esta sendo coberta pela PATH.

Eu uso a /usr/local/bin/

```
sudo ln -s caminho/seu/script /usr/local/bin/<nome>
```
